/**
 * GameState constructor.
 */
function GameState() {
}

GameState.prototype = Object.create(Object.prototype);
GameState.prototype.constructor = GameState;

/**
 * Init game state.
 * @param gameSet {GameSet} game set linked to this state.
 */
GameState.prototype.init = function(gameSet) {
	
}

/**
 * Display state elements.
 * @param gameSet {GameSet} game set linked to this state.
 */
GameState.prototype.display = function(gameSet) {
	
}

/**
 * Display state HUD.
 * @param gameSet {GameSet} game set linked to this state.
 */
GameState.prototype.displayHUD = function(gameSet) {
	
}

/**
 * Update state.
 * @param gameSet {GameSet} game set linked to this state.
 * @param currentTime {Integer} current time (in miliseconds).
 */
GameState.prototype.update = function(gameSet, currentTime) {
	
}